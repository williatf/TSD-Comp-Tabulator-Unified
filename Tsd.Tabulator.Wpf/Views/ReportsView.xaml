<UserControl x:Class="Tsd.Tabulator.Wpf.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             cal:Action.Target="None"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" ToolBarTray.IsLocked="True">
            <Button x:Name="Refresh"
                    Content="Refresh"
                    ToolTip="Reload report data from database"/>
        </ToolBar>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <!-- No Event Loaded Message -->
            <Border Background="#F5F5F5"
                    Visibility="{Binding HasEventLoaded, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="No Event Loaded"
                               FontSize="24"
                               FontWeight="Light"
                               Foreground="#888"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="Open an event from the Tabulation menu to view reports."
                               FontSize="14"
                               Foreground="#888"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Tabbed Reports -->
            <TabControl x:Name="Items"
                        Visibility="{Binding HasEventLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                         <TextBlock Text="{Binding DisplayName}"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentControl cal:View.Model="{Binding}"
                                       HorizontalContentAlignment="Stretch"
                                       VerticalContentAlignment="Stretch"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <!-- Loading Overlay -->
            <Border Background="#80FFFFFF"
                    Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Loading Reports..."
                               FontSize="18"
                               Foreground="#555"
                               Margin="0,0,0,10"/>
                    <ProgressBar IsIndeterminate="True"
                                Width="200"
                                Height="20"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
