<UserControl x:Class="Tsd.Tabulator.Wpf.Views.ReportTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Tsd.Tabulator.Wpf.Helpers"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <ItemsControl ItemsSource="{Binding Buckets}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,32">
                            <!-- Bucket Header -->
                            <TextBlock Text="{Binding Bucket}"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Margin="0,0,0,12"/>
                            <!-- Classes in Bucket -->
                            <ItemsControl ItemsSource="{Binding Classes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,24">
                                            <!-- Class Header -->
                                            <TextBlock Text="{Binding DisplayName}"
                                                       FontSize="16"
                                                       FontWeight="Bold"
                                                       Margin="0,0,0,8"/>
                                            <!-- DataGrid for Items -->
                                            <DataGrid ItemsSource="{Binding Items}"
                                                      AutoGenerateColumns="False"
                                                      CanUserAddRows="False"
                                                      CanUserDeleteRows="False"
                                                      helpers:DataGridColumnBinder.Columns="{Binding Columns}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>