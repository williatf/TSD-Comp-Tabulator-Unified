<UserControl x:Class="Tsd.Tabulator.Wpf.Views.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Tsd.Tabulator.Wpf.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1100">

    <UserControl.Resources>
        <Style TargetType="TextBox" x:Key="ScoreEntryBoxStyle">
            <EventSetter Event="PreviewKeyDown" Handler="ScoreEntryBox_PreviewKeyDown"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="1" Grid.Row="0">

            <TextBlock Padding="3" FontSize="18" HorizontalAlignment="Center"
                       Text="Trendsetters Dance Competition Tabulator - Unified"
                       VerticalAlignment="Top" />

            <!-- Selected Routine Header (add this) -->
            <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="0,8,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
            
                    <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold" Text="Entry ID: "/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding EntryId}"/>
            
                    <TextBlock Grid.Row="1" Grid.Column="0" FontWeight="Bold" Text="Routine: "/>
                    <TextBlock Grid.Row="1" Grid.Column="1">
                        <Run Text="{Binding RoutineTitle, Mode=OneWay}"/> - <Run Text="{Binding StudioName, Mode=OneWay}"/>
                    </TextBlock>
                </Grid>
            </Border>

            <!-- Tabs -->
            <TabControl ItemsSource="{Binding Tabs}"
                        SelectedItem="{Binding SelectedTab, Mode=TwoWay}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Header}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <views:ScoreSheetView />
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <!-- Submit / Cancel -->
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="75" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Padding="5" x:Name="Submit">Submit</Button>
                <Button Grid.Column="2" Padding="5" x:Name="Cancel">Cancel</Button>
            </Grid>

        </StackPanel>

        <!-- Routine list area (we'll wire this back to your real routines next) -->
        <Grid Grid.Column="1" Grid.Row="1">
            <DataGrid ItemsSource="{Binding Routines}" 
                      SelectedItem="{Binding SelectedRoutine, Mode=TwoWay}"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Entry ID" Binding="{Binding ProgramNumber}" Width="Auto"/>
                    <DataGridTextColumn Header="Entry Type" Binding="{Binding EntryType}" Width="Auto"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="Auto"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="Auto"/>
                    <DataGridTextColumn Header="Studio" Binding="{Binding StudioName}" Width="*"/>
                    <DataGridTextColumn Header="Routine" Binding="{Binding RoutineTitle}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Scored" Binding="{Binding IsScored}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
