<Window x:Class="Tsd.Tabulator.Wpf.Views.NewEventDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="http://www.caliburnproject.org"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="14" MinWidth="560">

        <TextBlock Text="Event Folder Name" FontWeight="SemiBold"/>
        <TextBox Margin="0,6,0,12"
                 Text="{Binding EventFolderName, UpdateSourceTrigger=PropertyChanged}"
                 Width="520"/>

        <TextBlock Text="Registration CSV" FontWeight="SemiBold"/>
        <DockPanel Margin="0,6,0,0">
            <TextBox Text="{Binding CsvPath}" IsReadOnly="True" Margin="0,0,8,0"/>
            <Button Content="Browse…" Padding="10,4" cal:Message.Attach="BrowseCsv"/>
        </DockPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Padding="12,5" Margin="0,0,8,0" cal:Message.Attach="Cancel"/>
            <Button Content="Create" Padding="12,5" IsEnabled="{Binding CanCreate}" cal:Message.Attach="Create"/>
        </StackPanel>
    </StackPanel>
</Window>
