<UserControl x:Class="Tsd.Tabulator.Wpf.Views.Reports.SoloAwardsReportTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" ToolBarTray.IsLocked="True">
            <Button x:Name="ExportCsv"
                    Content="Export CSV"
                    ToolTip="Export report to CSV file"/>
        </ToolBar>

        <!-- Error Message -->
        <Border Grid.Row="1"
                Background="#FFEBEE"
                BorderBrush="#F44336"
                BorderThickness="1"
                Padding="10"
                Margin="5"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠ "
                          FontSize="16"
                          Foreground="#D32F2F"
                          Margin="0,0,5,0"/>
                <TextBlock Text="{Binding ErrorMessage}"
                          TextWrapping="Wrap"
                          Foreground="#D32F2F"/>
            </StackPanel>
        </Border>

        <!-- Warning Message -->
        <Border Grid.Row="2"
                Background="#FFF9C4"
                BorderBrush="#FBC02D"
                BorderThickness="1"
                Padding="10"
                Margin="5"
                Visibility="{Binding HasWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠ "
                          FontSize="16"
                          Foreground="#F57C00"
                          Margin="0,0,5,0"/>
                <TextBlock Text="{Binding WarningMessage}"
                          TextWrapping="Wrap"
                          Foreground="#F57C00"/>
            </StackPanel>
        </Border>

        <!-- Report Content -->
        <ScrollViewer Grid.Row="3"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Report.Groups}"
                         Margin="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Group Header (Bucket + Class) -->
                        <StackPanel Margin="0,10,0,20">
                            <Border Background="#2196F3"
                                   Padding="10,5"
                                   Margin="0,0,0,5">
                                <TextBlock FontSize="16"
                                          FontWeight="Bold"
                                          Foreground="White">
                                    <Run Text="{Binding Bucket}"/>
                                    <Run Text="-"/>
                                    <Run Text="{Binding Class}"/>
                                </TextBlock>
                            </Border>

                            <!-- Entries Table -->
                            <DataGrid ItemsSource="{Binding Entries}"
                                     AutoGenerateColumns="False"
                                     IsReadOnly="True"
                                     GridLinesVisibility="Horizontal"
                                     HeadersVisibility="Column"
                                     CanUserResizeColumns="True"
                                     CanUserSortColumns="False"
                                     CanUserReorderColumns="False"
                                     SelectionMode="Single"
                                     AlternatingRowBackground="#F5F5F5">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Place"
                                                       Binding="{Binding Place}"
                                                       Width="60"/>
                                    <DataGridTextColumn Header="Score"
                                                       Binding="{Binding FinalScore, StringFormat=F2}"
                                                       Width="80"/>
                                    <DataGridTextColumn Header="Program #"
                                                       Binding="{Binding ProgramNumber}"
                                                       Width="90"/>
                                    <DataGridTextColumn Header="Participants"
                                                       Binding="{Binding Participants}"
                                                       Width="200"/>
                                    <DataGridTextColumn Header="Studio"
                                                       Binding="{Binding StudioName}"
                                                       Width="200"/>
                                    <DataGridTextColumn Header="Routine"
                                                       Binding="{Binding RoutineTitle}"
                                                       Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Indicator -->
        <Border Grid.Row="3"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <TextBlock Text="Loading Solo Awards..."
                          FontSize="16"
                          Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True"
                            Width="200"
                            Height="20"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
